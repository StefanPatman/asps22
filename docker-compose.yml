version: "3.7"

services:
  generator:
    image: generator
    environment:
      - INTERVAL=1
      - PROCESSOR=processor
      - PORT=5000

  generator_2:
    image: generator
    environment:
      - INTERVAL=1
      - PROCESSOR=processor
      - PORT=5000
      - LOCATION=Barcha
      - ID=2

  processor:
    image: processor
    environment:
      - A_PORT=5001
      - AGGREGATOR=aggregator
      - PORT=5000
    ports:
      - "5000:5000"

  aggregator:
    image: aggregator
    environment:
      - PORT=5001
    ports:
      - "5001:5001"
